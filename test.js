// La recuperation des elements 


// Fonstions
function team() {
    // Obtenir toutes les valeurs des inputs
    const player1 = "AA"

    console.log(player1);
    
let list = [];
if (player1 != "")
list.push(player1);
console.log(list);
}
